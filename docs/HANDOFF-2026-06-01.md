# Handoff — Sessão 2026-06-01

> **Para a próxima janela.** Estado completo do projeto + onde retomar.
> Última sessão: ~50 commits, foco em destravar loops de aprendizado +
> memória + redesign UI + bug fix crítico no backtest.

---

## 🎯 TL;DR para a próxima sessão

1. **Backtest descobriu: edge atual NEGATIVO** (Sharpe -4.58, WR 40%, avg loss 3.2× avg win). **NÃO escalar size** até calibrar.
2. **Loop de aprendizado fechado por completo** (Vision → trades → Mentor → mentor_applier → overrides → próxima Vision).
3. **Tudo é OPT-IN via .env.** Operador precisa habilitar flags + reboot.
4. **Server estava STALE** rodando desde 3:54 PM — endpoints novos só ativam após reboot.

---

## 🚦 AÇÃO IMEDIATA DO OPERADOR

`.env` é PROTECTED (eu não posso editar). Adicione/confirme:

```env
# === Vault writers (segundo cérebro) ===
PROMETHEUS_AGENT_ENABLED=true
PROMETHEUS_VAULT_WRITER_ENABLED=true
CABLE_AGENT_ENABLED=true
CABLE_VAULT_WRITER_ENABLED=true
MENTOR_VAULT_WRITER_ENABLED=true
CYCLOPS_VAULT_WRITER_ENABLED=true
VISION_VAULT_WRITER_ENABLED=true
STRATEGY_VAULT_WRITER_ENABLED=true
VAULT_MOC_UPDATER_ENABLED=true

# === Loops automáticos ===
AUTO_LEARNING_ENABLED=true        # auto_learning_scheduler em background
IMPLEMENTER_WORKER_ENABLED=true   # drena fila IMP a cada 15min
MENTOR_AUTO_APPLY_ENABLED=true    # aplica sugestões Mentor (gates triplos)

# === Throttle (já com defaults sensatos) ===
# BEAST_THROTTLE_MIN=15           # default — Beast cache 15min
# IMPLEMENTER_WORKER_INTERVAL_MIN=15
# IMPLEMENTER_WORKER_MAX_PER_CYCLE=3
# IMPLEMENTER_WORKER_AUTO_APPLY=false  # MANTENHA FALSE até validar dry-run runs
```

Depois: **Hard Reboot** (UI-A botão no menu) ou `python run.py --dashboard` fresh.

---

## 📊 ESTADO ATUAL DO SISTEMA

### Memória / Aprendizado (audit_log últimos 7 dias)

| Event | Count | Status |
|---|---|---|
| BEAST_PROPOSAL | 2445 | Beast hyperativo (mas throttle implementado, vai cair pra ~4/h após reboot) |
| MONITOR_RECOVERY_PLAN | 1313 | OK |
| SIGNAL_FLIP | 310 | Vision flipando — esperado |
| RISK_REJECTED | 308 | Batman ativo |
| AUTO_LEARNING_CYCLE | 246 | Scheduler rodando |
| DECISION_MEMORY | 209 | Decisões gravadas |
| DECISION_OUTCOME | 209 | ✅ 100% closure (janitor resolveu retroativos) |
| TRADE_NOW_BLOCKED | 85 | Manual trades bloqueados (Batman gates) |

### Trades reais (`trades` table)
- 54 trades totais (BTC dominante)
- Status mix: ERROR 22, FILLED 19, PAPER 4, REJECTED 3, SKIPPED 6
- **Backtest BTC 7 dias**: Sharpe **-4.58**, WR 40%, MaxDD 2%, avg loss $39 vs avg win $12

### Vault canônico (`~/Documents/mekka-trading-obsidian`)
- 176 notas em 31 pastas
- Health score: 86/100 (após organização)
- 3 broken links residuais (placeholders Templater intencionais)
- 8 daily notes gaps (operator manual)

### Office v4 visual
- Corredores 4-5× mais largos (commit `26aafc0`)
- Sprites se desviam (separation steering + idle drift)
- Cache buster `v=20260529c-corridors`

---

## 🏗️ TUDO QUE FOI FEITO NESTA SESSÃO

### Wave 1 — INV-1..18 (memory loop)
- `signal_outcome_janitor.py` (resolve PENDING agent_memories)
- `decision_memory_janitor.py` (resolve DECISION_MEMORY orphans)
- Beast audit event + Cable audit event
- ImplementerWorker IMPLEMENTER_RAN audit
- 7 deterministic patterns (2 → 7)
- Blast radius 5/500 → 8/1000
- Mentor MENTOR_RAN + MENTOR_SUGGESTED
- BeWater BE_WATER_DECISION audit
- Vision dual vault context (trend + regime playbook)
- Decision Memory closure wired
- Vault Activity widget endpoint
- Strategy Selector wire-in com Sharpe

### Wave 2 — PROM-A..E (Prometheus)
- Auto-start no boot do dashboard
- Learnings → audit_log (PROMETHEUS_LEARNING)
- KPIs enriquecidos (top_error_agents, action_mix, error_rate)
- `/api/prometheus/snapshot`
- Beast lê Prometheus learnings → propõe melhorias

### Wave 3 — TRADE/COIN/SCALP fixes
- Telegram gate em `/api/trade/execute`
- Symbol validation
- Rate limit manual trades
- pnl_quote fallback COIN-M
- Per-symbol leverage caps COIN-M
- Flash disagreement hard-block em scalp
- ATR-dynamic trades-per-hour cap

### Wave 4 — VAULT reorganização
- `vault_auditor.py` + `/api/vault/audit`
- Health 28 → 86
- Entry points consolidados (Home como SoT)
- Duplicata Review Semanal arquivada
- Vision Critic note criada
- Whitelist órfãos intencionais (Stories, Daily, regime-log)

### Wave 5 — Office v4
- Separation steering (collision avoidance)
- Idle drift (parados também desviam)
- Corredores 20px → 80-100px (4-5× maior)
- HALL_Y 970 → 1010
- 32 sprites repositionados nas novas bounds

### Wave 6 — MEM-AUDIT-1..4 + MEM-FIX-5..8
- `/api/server/health` + DASHBOARD_BOOT audit
- VAULT_WRITERS_STATE audit (mostra quais flags off)
- Beast throttle classe-level (60/h → 4/h, 13.279× speedup)
- Janitor mapping correto (TRADE_NOW_EXECUTED real, não IRONMAN_FILLED)
- Janitor fallback via `trades` table
- Regex `add_test_stub` expandido (case "29 agente(s)")

### Wave 7 — A→D paralelos
- **A**: Backtest signal loader BUG (timestamp_utc vs timestamp) — fixed; relatório real gerado
- **B**: `mentor_applier.py` — gates triplos, opt-in, idempotente, wired no auto-learning
- **C**: Scanner thresholds (lazy 4→8, CSS 20→50 + whitelist 16 prefixos)
- **D**: `learning_hub.js` widget unificado (4 endpoints em paralelo)

---

## 🔌 ENDPOINTS NOVOS (ativam após reboot)

| Endpoint | Função |
|---|---|
| `GET /api/server/health` | pid, uptime, tasks, endpoints registrados |
| `GET /api/prometheus/snapshot` | enabled, snapshot, audit count |
| `GET /api/vault/activity` | writers ativos, writes 7d, last write |
| `GET /api/vault/audit` | health score, órfãos, broken links, gaps |
| `GET /api/implementer/status` | background + history |
| `POST /api/implementer/run-once` | manual trigger (body: max_imps, dry_run) |
| `POST /api/decision-memory/janitor` | resolve orphans on demand |
| `GET /api/cable/snapshot` | Cable derivatives intel (já existia) |

---

## 🧠 LOOPS DE APRENDIZADO ATIVOS

```
1. Vision/Batman/IronMan operam com settings + mentor_overrides
2. Trades fecham (paper/live) → tabela trades
3. Janitor + trade_outcome_resolver fecham:
     - agent_memory (Story 063)
     - role_working (Story 183)
     - signal_outcome (Story 186)
     - decision_memory (INV-15)
4. Auto-learning scheduler (15min loop) roda:
     - stale_imp_learner
     - signal_outcome_janitor (INV-1)
     - memory_reconciler
     - mentor.run()
     - mentor_applier (INV-14)        ← NOVO loop completo
     - cable_vault_writer
5. Mentor sugere → high_conf inbox → mentor_applier
6. Overrides em data/mentor_overrides.json (não toca settings.py PROTECTED)
7. Próximo Vision/Batman lê via get_override("min_confidence", default)
8. Prometheus observa event_bus → emite PROMETHEUS_LEARNING
9. Beast lê PROMETHEUS_LEARNING → propõe ImprovementProposals
10. Mekka.run() consolida proposals → improvement_queue
11. ImplementerWorker drena queue (15min) → deterministic patterns
12. IMPs commitadas → squad audit + git history
```

---

## ⚠️ GAPS CONHECIDOS / RISCOS

1. **Edge negativo no backtest** — Sharpe -4.58 em BTC 7d. Antes de produção:
   - Calibrar `min_confidence` ↑ (sugestão: 0.75)
   - Investigar por que avg loss = 3.2× avg win (TP muito apertado? SL muito largo?)
   - Rodar backtest em outros símbolos quando dados acumularem
2. **Dados históricos limitados** — só 4 dias no DB. Fetch via CCXT/Hyperliquid pra backtest 30/90d.
3. **Mentor auto-apply** — só direção `tighten` aplica. Loosen sempre bloqueado.
4. **IMPLEMENTER_WORKER_AUTO_APPLY=false default** — drena queue em dry-run. Operator deve manualmente revisar antes de ligar AUTO_APPLY.
5. **Server reboot necessário** — endpoints novos só ativam após restart.

---

## 📋 BACKLOG PRIORIZADO PARA PRÓXIMA SESSÃO

### Alta prioridade
- [ ] **Investigar edge negativo do backtest** — diagnóstico profundo do por que avg loss = 3.2× avg win
- [ ] **Fetch dados históricos 30-90d** via CCXT/Hyperliquid pra backtest sério
- [ ] **Validar mentor_applier em produção** — após operator habilitar, observar 1 ciclo completo

### Média prioridade
- [ ] **Refinar scanners** (Agent C recomendou consolidar 8 scanners em base class comum)
- [ ] **JeanGrey duplicate detection** — substituir SequenceMatcher por SHA256 hash dedup
- [ ] **Frontend orphan CSS** — migrar para PurgeCSS dedicado (heurística atual ainda tem FP)
- [ ] **Decision Memory closure dual-write** — adicionar emit em Cyclops/IronMan, não só janitor

### Baixa prioridade
- [ ] **Beast HEALTH score refinement** — score atual 35-95 oscila muito
- [ ] **Office v4 polish** — adicionar interações entre sprites (high-five, water cooler chat)
- [ ] **Dashboard widget** — `mentor_overrides.json` visível na UI

---

## 🛠️ COMANDOS ÚTEIS PARA A PRÓXIMA SESSÃO

```bash
# Estado live
sqlite3 data/mekka_trading.db "SELECT event, COUNT(*) FROM audit_log WHERE timestamp >= datetime('now','-1 day') GROUP BY event ORDER BY 2 DESC LIMIT 10"

# Endpoints health
curl http://localhost:8787/api/server/health | jq
curl http://localhost:8787/api/prometheus/snapshot | jq
curl http://localhost:8787/api/vault/activity | jq
curl http://localhost:8787/api/implementer/status | jq
curl http://localhost:8787/api/vault/audit | jq '.health_score, .issues'

# Janitor manual
python -m src.services.decision_memory_janitor --apply --limit 200

# Mentor applier (dry-run, sempre seguro)
python -m src.services.mentor_applier
python -m src.services.mentor_applier --status

# Implementer manual run
python -m src.services.implementer.worker --max 3 --dry-run

# Vault audit
python -m src.services.vault_auditor --summary

# Backtest (precisa de dados — atualmente só 4 dias)
python -m src.backtest run --symbol BTC --days 7 --initial-equity 10000 --seed 42
```

---

## 🗺️ ARQUIVOS-CHAVE PARA NAVEGAÇÃO

### Memória / Aprendizado
- `src/services/decision_memory.py` — Story 249
- `src/services/decision_memory_janitor.py` — INV-3 + MEM-AUDIT-3
- `src/services/signal_outcome_janitor.py` — INV-1
- `src/services/trade_outcome_resolver.py` — Story 186 + INV-4/15
- `src/services/mentor_applier.py` — INV-14
- `src/services/auto_learning_scheduler.py` — REV-3
- `src/agents/mentor.py` — Charles Xavier
- `src/agents/prometheus.py` — observer
- `src/agents/beast.py` — system audit (throttle classe-level)

### Backtest
- `src/services/backtest_runner.py`
- `src/services/backtest_signal_loader.py` — **bug fix `timestamp_utc`**
- `src/services/backtest_metrics_engine.py`
- `src/strategies/scalp.py` + `scalp_aggressive.py`
- `reports/backtest_scalp_7d.md` — relatório real

### Dashboard
- `src/dashboard/server.py` — endpoints + boot
- `src/dashboard/static/learning_hub.js` — widget unificado D
- `src/dashboard/static/memory_hub.js` — 6 camadas
- `src/dashboard/static/office_v4/` — corredores alargados

### Scanners (Agent C identificou gaps)
- `src/agents/agents_scanner.py` — agentes
- `src/agents/backend_scanner.py` — Python (threshold lazy ↑ 4→8)
- `src/agents/frontend_scanner.py` — HTML/CSS/JS (whitelist dinâmica)
- `src/agents/code_auditor.py` — repo
- `src/agents/dashboard_scanner.py` — server.py
- `src/agents/risk_scanner.py` — risk posture
- `src/agents/ops_scanner.py` — ops events
- `src/agents/jean_grey.py` — vault (SequenceMatcher → precisa refactor)

### Vault
- `src/services/vault_auditor.py` — health score
- `src/services/vault_context.py` — Vision reader
- `src/services/cable_vault_writer.py`
- `src/services/strategy_vault_writer.py`
- `src/services/trading_vault_writer.py` — Mentor/Cyclops/Vision
- `src/services/prometheus_vault_writer.py`
- `~/Documents/mekka-trading-obsidian/` — canonical vault

---

## 📁 ESTRUTURA FRAMEWORK AIOS

Lembrete: `src/config/settings.py`, `src/agents/batman.py`, `src/agents/iron_man.py`, `.env`, `.env.example` são **PROTECTED**. Toda mudança nesses arquivos precisa de approval explícito do operador.

Resto do `src/` é livre para editar com cuidado.

---

## 🔄 CONTINUIDADE

Próxima sessão começa abrindo:
1. Este arquivo `docs/HANDOFF-2026-06-01.md`
2. `git log --oneline -20` para revisar últimos commits
3. `curl http://localhost:8787/api/server/health` para checar uptime/tasks
4. Verificar se operator habilitou as flags do `.env` (compare com VAULT_WRITERS_STATE audit event)

**Estado mental:** sistema está **operacional mas com edge negativo no backtest**. Próxima onda deve ser **diagnóstico do edge** — não escalar até entender por que avg_loss = 3.2× avg_win.

---

🤖 Generated 2026-06-01 by Claude Opus 4.7 (1M context)
